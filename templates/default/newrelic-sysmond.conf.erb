# start newrelic-sysmond.conf
description "Start New Relic System Monitoring"

start on (local-filesystems and net-device-up IFACE!=lo)
stop on runlevel [!2345]

respawn
respawn limit 5 20
expect fork

# start the daemon
exec /usr/local/sbin/nrsysmond